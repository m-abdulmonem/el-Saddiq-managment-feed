        <div class="col-6"
                     :id="result.id"
                     :num="result.num"
                     :name="result.name"
                     :code="result.code"
                     :price="result.price"
                     :stocks="JSON.stringify(result.stocks)"
                     :quantity="result.quantity"
                     :profit="result.profit"
                     :admin="result.admin"
                     :discount="result.discount">{{ result.name }}</div>
                <div class="col-2"
                     :id="result.id"
                     :num="result.num"
                     :name="result.name"
                     :code="result.code"
                     :price="result.price"
                     :stocks="JSON.stringify(result.stocks)"
                     :quantity="result.quantity"
                     :profit="result.profit"
                     :admin="result.admin"
                     :discount="result.discount">{{ result.quantity}}</div>
                <div class="col-2"
                     :id="result.id"
                     :num="result.num"
                     :name="result.name"
                     :code="result.code"
                     :price="result.price"
                     :stocks="JSON.stringify(result.stocks)"
                     :quantity="result.quantity"
                     :profit="result.profit"
                     :admin="result.admin"
                     :discount="result.discount">{{ currencies(result.price)}}</div>
                <div class="col-2"
                     :id="result.id"
                     :num="result.num"
                     :name="result.name"
                     :code="result.code"
                     :price="result.price"
                     :stocks="JSON.stringify(result.stocks)"
                     :quantity="result.quantity"
                     :profit="result.profit"
                     :admin="result.admin"
                     :discount="result.discount">{{ result.weight ? result.weight + " ك ": ""}}</div>
